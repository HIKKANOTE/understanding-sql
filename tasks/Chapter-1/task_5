/* Запишите запрос, позволяющий получить значения столбца snum для всех
продавцов (salespeople ), номера ( orders) которых находятся в настоящее время в таблице
Orders, причем повторения требуется исключить. */

SELECT DISTINCT snum FROM Orders

-- DISTINCT исключает повторения
